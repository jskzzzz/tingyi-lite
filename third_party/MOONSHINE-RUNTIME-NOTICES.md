# Third-Party Notices

This runtime directory contains:

- Moonshine Voice runtime and helper-facing API from Moonshine AI. See `LICENSE.moonshine-voice.txt`.
- ONNX Runtime native library. See `LICENSE.onnxruntime.txt`.
- Microsoft Visual C++ runtime redistributable files: `concrt140.dll`, `msvcp140.dll`, `vcruntime140.dll`, and `vcruntime140_1.dll`. These files were copied from the Visual Studio 2022 VC143 x64 redistributable directory and remain subject to the Microsoft Visual Studio licensing and redistribution terms.

The exact delivered files and SHA-256 hashes are recorded in `runtime-manifest.json`.
